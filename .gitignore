# Local credentials — never committed.
.env
.env.*
!.env.example

# Python build artefacts.
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Local working state.
.coverage
coverage.json
pyright_output.json
base_budget.json
.pytest_cache/
.ruff_cache/
.venv/
# Sibling libs (Praxis, Nexus, Limen) are intentionally unpinned in
# pyproject.toml so every install pulls their latest main. uv.lock
# would re-pin them to specific SHAs and silently freeze the contract.
uv.lock

# IDE / OS / agent.
.DS_Store
.idea/
.vscode/
.ipynb_checkpoints/
.claude/

# Tunneled / CSV artefacts (operator-local sweep outputs).
e2e.csv
min.csv
tiny.csv
*.csv
# Slice 1 (#?): canonical golden-test fixtures must ship in-repo.
!tests/fixtures/canonical/expected/sweep_per_window.csv
!tests/fixtures/canonical/expected/sweep_per_tick.csv
Untitled.ipynb
