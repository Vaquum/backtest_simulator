# Project-root .env consumed by `bts run` / `bts sweep`.
# Copy to `.env` (gitignored) and fill in the password from the
# tdw control-plane host (`/opt/tdw-control-plane/.env`).
CLICKHOUSE_PASSWORD=
