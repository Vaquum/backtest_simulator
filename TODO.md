# TODO — backtest_simulator honest market adaptation

The plan: drive the existing pipeline (UEL → ManifestBuilder → BacktestLauncher → SimulatedVenueAdapter against real ClickHouse trade ticks) so the trading results visibly reflect a real long-only binary-regime strategy responding to a real market. Then in Part 2, add the mechanical honesty gates (stops, conservation, validator, etc.). Part 3 (M2) takes the remaining honesty / capability / test gaps to closure under a single slice — issue #17.

**Constraint that overrides everything: the profile run stays at ≈10 seconds total. Faster is welcome. Slower is not.**

---

## Part 1 — pipeline behaves like the real market, results visible

- [x] **SSH tunnel preflight.** The profile script opens an SSH tunnel to the internal ClickHouse host and verifies the tunnel by querying ClickHouse for one recent BTCUSDT trade before booting the launcher. If the tunnel is down, the script fails loud with the exact reopen command. Connection details live in operator-local environment/config, not in the repo.
- [x] **ClickHouseFeed wired to `origo.binance_daily_spot_trades`.** `database='origo'`, `table='binance_daily_spot_trades'` defaults. Format datetime parameters as `'%Y-%m-%d %H:%M:%S.%f'` (DateTime64(6) rejects ISO-T+timezone). Real Binance ticks reach the venue adapter.
- [x] **Strategy template — preds-based binary regime, long-only.** `self._long: bool` state, persisted via `on_save`/`on_load`. `preds=1` AND flat → ENTER BUY. `preds=0` AND long → ENTER SELL with the entry qty. `preds=1` AND long → no-op. `preds=0` AND flat → no-op. No probs, no enter_threshold.
- [x] **`StrategyParamsSpec` updated.** Drop `qty` and `enter_threshold`. Add `capital: Decimal`, `kelly_pct: Decimal`, `estimated_price: Decimal`. ManifestBuilder bakes them into the generated strategy file.
- [x] **Filter API supports column ranges.** `filter_results({'col': (lo, hi)})` keeps only rows where `lo ≤ col ≤ hi`. Equality and set-membership stay supported. *(already supported by `_column_predicate` — tuple-of-2 triggers the range branch.)*
- [x] **Synchronous drain after every submit.** `_advance_clock_until` waits until each submitted `command_id` has been dispatched by Praxis's `account_loop` before advancing the next clock tick.
- [x] **Profile script wired to ClickHouse + Kelly.** Picks one active decoder via the new range-filter on `backtest_trades_count`, reads its `backtest_mean_kelly_pct`, fetches a seed price, runs the 14h window. Prints per-trade entry/exit prices, sides, qty, fees.
- [x] **Verify in the run output:** `orders > 0`, `trades > 0`, with `BUY` + `SELL` pairs corresponding to `preds 0→1→0` transitions, fill prices and times matching real Binance ticks. Total wall time ≈10s.
- [x] **Commit + push to `feat/m1-bootstrap`.** *(commit `f63d1f6`.)*

## Part 2 — honesty hardening (Issue #10 invariants)

- [x] Real `ValidationPipeline` (CAPITAL=real, others=_allow). *(`backtest_simulator/honesty/capital.py::build_validation_pipeline`)*
- [x] CapitalController 4-step lifecycle (`check_and_reserve → send_order → order_ack → order_fill`).
- [x] Conservation laws checked after every event (`assert_conservation`). *(INV-1a/1b/2/3.)*
- [x] Declared-stop enforcement; INTAKE rejects stop-less ENTER.
- [x] `r_per_trade` from declared stop (no virtual `stop_bps` denominator).
- [x] `is_maker` reflects real fill type. *(MARKET orders are always taker → `False` is correct; documented.)*
- [ ] **`SignalsTable` precompute layer + per-decoder split-alignment.** Deferred from Part 2 to Part 3 (M2) Task 16, where `SignalsTable.lookup` gains `path_id` / `purge_seconds` / `embargo_seconds` and is wired into the live predict path.
- [x] 75 honesty tests across 12 files in `tests/honesty/`.
- [x] `pr_checks_honesty` workflow added; live ruleset includes it as a required check.
- [x] `bts enrich` CLI joins `results.csv` with optional `backtest_results.parquet`. *(Now part of the master `bts` CLI; see Part 3 Task 1.)*

## Part 3 — M2 (slice issue #17) — package the `bts` master CLI and close every open honesty / capability / test gap

> **Operational protocol (operator-mandated)**
>
> 1. **CLI is the master tool.** All debugging through `bts`. Test runs through `bts test`. Lint through `bts lint`. Typing through `bts typecheck`. Gates through `bts gate <name>`. Verbosity flags `-v`, `-vv`, `-vvv` accepted on every subcommand. **No tool other than `bts`, `codex`, `git`, and `gh` is used over the lifetime of this slice; any direct `pytest` / `ruff` / `pyright` / `python -c` invocation outside `bts` is a workflow violation.**
> 2. **One slice (#17), one PR, 25 commits.** Each task is one commit. Each commit is reviewed by codex (model 5.5, reasoning xhigh) until codex approves. Each approved commit is pushed to the slice's PR before the next task starts. Exactly one PR for the whole slice.
> 3. **Codex first, zero-bang last.** Codex reviews every commit. After all 25 commits land, codex reviews the PR end-to-end and the implementer iterates until codex approves the PR. Only then is zero-bang re-requested. Implementer addresses zero-bang's comments and re-requests until zero-bang approves.
> 4. **Operator review is the final gate.** Operator pinged ONLY when the GitHub merge button is truly green (10 gates green AND `reviewDecision == APPROVED` AND every review thread resolved). Not before.
> 5. **No empty scaffolding.** Every task lands fully-functional code. No placeholders, no stubs. Each task's exit-state must be verifiable via the CLI without additional setup.
> 6. **TODO.md is the live mirror.** Every checkbox below reflects current truth at the latest commit on this branch. The CLI move (Task 1) updates this file and adds the 25-task list; subsequent commits tick their box.
> 7. **Closest-possible-market-simulation standard.** Every modeling decision (fills, stops, slippage, maker/taker, validator sequence, R metric) matches live as tightly as the available data allows. Where data cannot resolve a detail, the model falls back to the conservative (worse-for-strategy) choice. Backtest ≡ paper ≡ live byte-identical event spine is the M3 unlock.

### Tasks (1–25)

- [ ] **Task 1.** Move `/tmp/bts_sweep.py` into the package as `bts` master CLI: subcommands `run`, `sweep`, `enrich`, `test`, `lint`, `typecheck`, `gate`, `notebook`, `version`; verbosity `-v`/`-vv`/`-vvv`; `docs/cli.md`; `tests/cli/`; pyproject 1.5.2 → 1.6.0; CHANGELOG; this TODO.md update.
- [ ] **Task 2.** `test_sanity_zero_trade` — no-op strategy emits no fills, no fees.
- [ ] **Task 3.** `test_sanity_buy_hold` — return within ±5 bps of `(close−open)/open − fees`.
- [ ] **Task 4.** `test_sanity_over_trading` — over-trading models post mean R/trade < 0, PF < 1.
- [ ] **Task 5.** `test_sanity_random` — 1000 random seeds, mean return 95% CI brackets 0.
- [ ] **Task 6.** `test_prescient_strategy` — prescient strategy raises `LookAheadViolation`.
- [ ] **Task 7.** `test_inverse_prescient` — sign-flipped prescient posts catastrophic loss.
- [ ] **Task 8.** `test_shuffle_bars` — permuted-bar-order profit collapses to ≈0 after fees.
- [ ] **Task 9.** `test_on_save_purity` — `save → load → save` produces identical bytes.
- [ ] **Task 10.** R-denominator gameability gate — ATR-based stop-distance check; `stop = entry × 0.9999` is rejected.
- [ ] **Task 11.** Book-gap instrumentation — max stop-cross-to-trade gap reported per-run; surfaced via `bts run --output-format json`.
- [x] **Task 12.** Slippage model — calibrated empirical slippage from the same symbol/period's trade tape, *measured against rolling mid* per taker fill (NOT applied to fill_price; the audit rejected the adjust-fill_price design as double-counting `walk_trades`'s already-tape-priced fills). bts sweep per-run line shows `slip <±cost_bps> n=<count>/excl=<count>`; bts run JSON reports six fields (signed mean, side-normalized cost, BUY mean, SELL mean, n_samples, n_excluded). Commits 0709636 (primitive), 53ad757 (initial wiring), 6893b2d (audit fix 1 — measure-only, side-normalized, n_excluded counter), d3c54bc (audit fix 2 — sweep visibility, cost-bps math, side-normalized cost replaces `mean(|bps|)`).
- [ ] **Task 13.** Market impact model — per-$-traded penalty derived from tape volume distribution; flag for orders exceeding `threshold_fraction` of concurrent trade volume.
- [ ] **Task 14.** Passive maker-fill realism — queue position bound from trade stream; partial fills; aggressor-size bound; LIMIT-OPEN handling in launcher.
- [ ] **Task 15.** `test_sanity_maker_no_fill` — far-away maker orders never fill (depends on Task 14).
- [ ] **Task 16.** `SignalsTable.lookup(t, *, path_id=None, purge_seconds=0, embargo_seconds=0)` — wired into the live predict path; CPCV index + per-decoder split alignment.
- [x] **Task 17.** Statistical honesty — Deflated Sharpe + Probability of Backtest Overfitting + Superior Predictive Ability test wired into `bts sweep` summary. **Landed (DSR + SPA + PBO portion).** New module `cli/_stats.py` — `daily_return_for_run` (returns None on trailing inventory; codex round 1 P1 — runs with open positions excluded from stats so apparent Sharpe isn't inflated by treating unrealised PnL as zero), `fetch_buy_hold_benchmark` (per-day open/close fetched via `seed_price_at`), `compute_sweep_stats(n_search_trials=...)`. DSR uses `--n-permutations` as the multiple-testing inflation factor (codex round 1 P1 — visible pick count under-deflates). PBO skips on pairwise-identical decoder series (codex round 1 P1 — primitive's deterministic tie ordering would falsely report `pbo=0.000`). 12 new tests pin the contracts. **CPCV portion deferred until Task 16 lands** — without path-aware `SignalsTable.lookup` wiring, CPCV math would be decorative (printed off same per-run returns as the other stats, no real path-awareness). Visible follow-up tracked in CHANGELOG.
- [ ] **Task 18.** Ledger parity vs Praxis — byte-identical event-spine assertion between backtest and paper-Praxis replay.
- [ ] **Task 19.** `test_perf_gate_logreg_binary` — 1-year hourly replay < 10 s wall time on a frozen parquet fixture.
- [ ] **Task 20.** Parametric thresholds — `pytest.param` + env-var override across all calibrated honesty tests; paired calibration-proof tests.
- [ ] **Task 21.** 5 separate `_mutation.py` files (perf, conservation, split-alignment, on-save, determinism) — each test fires loud when the mutation it is meant to catch is applied.
- [ ] **Task 22.** `NexusRuntime` module — facade composing `ValidationPipeline` + `CapitalController` + `ActionSubmitter` + `CapitalState`; `submit(action)` returns `SubmissionOutcome`; `assert_conservation(context)` honors INV-1/2/3.
- [ ] **Task 23.** `SimulationDriver` module — drives `BacktestLauncher` end-to-end against `NexusRuntime` + `SimulatedVenueAdapter` + `HistoricalFeed`; `run(window_start, window_end)` returns `RunReport`.
- [ ] **Task 24.** 3 integration test files: `test_end_to_end`, `test_cli_end_to_end`, `test_notebook_execution`.
- [ ] **Task 25.** `notebooks/sweep_and_analyze.ipynb` — operator-facing demo notebook; `jupyter nbconvert --to script | python -` runs end-to-end.

### P0 architectural honesty gaps surfaced mid-slice (added 2026-04-25)

- [ ] **Task 26.** Outcome-driven strategy state. `StrategyContext.positions` and `capital_available` are populated from real `CapitalState` + `Account` (currently empty / zero in `launcher.py:687`). Strategy templates flip `_long` / `_entry_qty` only inside `on_outcome` after a confirmed fill (currently flipped at action-emit time in over_trading / buy_and_hold). Partial fills surface back so the strategy emits a corrective close for the *actual* filled qty. Without this fix a rejection or partial-fill leaves the strategy believing it owns a position it does not, then later emits a SELL for the wrong quantity.
- [x] **Task 27.** SELL closes have a canonical close lifecycle. **Landed via 9808fcf (decision: NOT through `validation_pipeline.validate`).** The original Task 27 wording ("route SELL through `validate`") was based on the assumption that `CapitalController` exposes a close-position / position-decrement primitive that the CAPITAL stage could drive on EXIT actions. It does not — `CapitalController` has only `check_and_reserve → send_order → order_ack → order_fill → order_reject / order_cancel`; `order_fill` adds to `position_notional` (it cannot subtract), and the CAPITAL stage's only operation is to RESERVE new capital. Routing SELL through `validate` would either reserve capital on every close (over-committing) or no-op (the current CAPITAL skip), neither of which is the canonical close lifecycle. The codex round 5 review confirmed: `capital_pool` is the immutable budget, SELL proceeds are realized PnL not new budget, and the close must release `position_notional` (not credit `capital_pool`). Final design: `CapitalLifecycleTracker.record_open_position` (called after each BUY fill) appends to a FIFO ledger; `CapitalLifecycleTracker.record_close_position(capital_state, sell_command_id, sell_qty, sell_proceeds, sell_fees)` (called after each SELL fill in `_finalize_sell_close`) mirrors `CapitalController.order_fill` exactly — releases `cost_basis + entry_fees` from `position_notional` and decrements `per_strategy_deployed[strategy_id]` by the same amount. Partial SELL shrinks the head proportionally (codex round 5 P2). Conservation INV-1 (`capital_pool` non-increasing) holds because we don't touch `capital_pool`. Realized PnL is logged on the lifecycle line and surfaces as the trade-pair PnL in `cli/_metrics.print_run`. Action_submitter's `SELL ENTER` short-circuit stays — it remains the BTS-side fast-path that skips reservation; the canonical close lifecycle now runs in the launcher's adapter wrapper post-fill.
- [ ] **Task 28.** Scope `threading.Timer.run` patch to `accelerated_clock`. `launcher/clock.py:117` does `setattr(threading.Timer, 'run', _frozen_aware_timer_run)` at module import — any process that imports `backtest_simulator` gets timer semantics globally rewritten, including paper/live code that imports the package for *any* reason. Move the patch inside the `accelerated_clock` context manager (save the original and restore on exit) so the rewrite is scoped to the frozen-clock backtest only.
- [x] **Task 29.** Wire `AtrSanityGate` into `action_submitter._check_declared_stop`. **Landed.** Final design: (a) new `compute_atr_from_tape(trades_pre_decision, period_seconds=60)` helper on `honesty/atr.py` produces ATR as mean of per-1-min ranges over a 900s strict-causal pre-decision slice. (b) New `_check_atr_sanity` in `action_submitter.py`, symmetric to `_check_declared_stop`, called after it in `_submit_translated`. Returns `ValidationDecision(allowed=False, reason_code='ATR_<reason>')` on denial. (c) Entry-price priority (codex round 1 P1): refreshed LIMIT `execution_params['price']` → `touch_provider(symbol)` → stale `action.reference_price`. (d) `SubmitterBindings` gains `atr_gate` + `atr_provider` (default None disables); new `AtrRejectHook` + `on_atr_reject` kwarg on `build_action_submitter`. (e) `BacktestLauncher` gains `atr_gate` + `atr_provider` ctor kwargs, `n_atr_rejected` + `n_atr_uncalibrated` properties, `_record_atr_rejection` hook. (f) `cli/_run_window.py` constructs the gate (`window=900s, k=0.5`) and provider (closes over feed). (g) `bts run --output-format json` emits `n_atr_rejected` / `n_atr_uncalibrated`; `bts sweep` per-run line carries `atr_rej=N/uncal=M` when non-zero; sweep summary carries `sweep atr rejected=N uncalibrated=M` when non-zero. (h) 11 new tests, mutation-proof boundary on entry-price priority. Long-only template scope (BUY entries only); short-side strategies need explicit intent plumbing when they land. Auditor non-blocker: `k` and `window_seconds` are hard-coded; CLI flags are a control follow-up. Codex 5.5 xhigh approved over 3 rounds; auditor: Task 29 RESOLVED.
- [ ] **Task 30.** Wire `BookGapInstrument` into `venue/fills.py::_walk_stop` and propagate the snapshot to `bts run --output-format json`. The instrument (`backtest_simulator/honesty/book_gap.py`) ships standalone with its MVC test pinning the `record → snapshot → metric` contract, but venue stop fills do not call `record_stop_cross`, the child run report does not include `BookGapMetric`, and `bts run --output-format json` still emits `book_gap_max_seconds: None`. Task 30 plumbs (a) the instrument into `_walk_stop` keyed on the cross/first-trade timestamps, (b) the snapshot into `cli/_run_window.run_window_in_process`'s return value, and (c) the metric fields into `cli/commands/run._build_json_report`. Codex Task 11 round 2 surfaced this; the standalone-correctness test stands, Task 30 closes the wiring loop into the operator-visible JSON.
- [x] **Task 31.** Wire `MarketImpactModel` into the simulator's pre-fill check and propagate the realised impact bps + flag to `bts run --output-format json`. **Landed.** Final design (post-audit, codex round 2): (a) per-submit STRICT-CAUSAL inlined estimate — each ENTER fetches `[submit_time - bucket_minutes, submit_time)` of pre-submit tape (filtered post-fetch to `time < submit_time` since the feed's range query may be inclusive) and computes the standalone model's linear-interpolation math (`total_volume`, `price_range_bps`, `impact_bps`, `flag`) inline against the rolling slice — no `MarketImpactModel.calibrate` call from the venue path, since the standalone calibration truncates to wall-clock minute boundaries and would split a non-boundary submit's trailing minute across two partial buckets. The standalone primitive ships unchanged for direct operator use / forensics. (b) `_record_market_impact_pre_fill` per ENTER records impact_bps + flag in running aggregates; when `--strict-impact` is set AND the order is flagged as exceeding `threshold_fraction` (default 10%) of concurrent volume, the venue routes to `OrderStatus.REJECTED` BEFORE walk_trades — the auditor's pre-fill gate semantic. (c) `bts run --output-format json` emits `market_impact_realised_bps`, `market_impact_n_samples`, `market_impact_n_flagged`, `market_impact_n_uncalibrated`, `market_impact_n_rejected`. (d) `bts sweep` per-run line carries `imp <±bps>bp n=<N>/flagged=<F>` and `bts sweep` summary aggregates `mean=<bps>bp n=<total> flagged=<total> rejected=<total> uncalibrated=<total>` with WARN suffixes when calibration gap >= 10% or flagged fraction >= 5%. (e) New CLI flag `--strict-impact` on both `bts run` and `bts sweep`; default observability mode keeps the prior measurement-only behaviour. Commits: 0b6e9f6 (initial wiring + audit P1 lookahead), follow-up with strict-causal per-submit inline estimate + pre-fill gate.
- [x] **Task 32.** Wire `SlippageModel` into the venue path and surface realised bps on `bts sweep`. **Landed.** Final design (post-audit): the adapter does NOT adjust `fill_price` (walk_trades already returns realistic taker prices from the historical tape; multiplying by bps doubles the spread effect). Instead, for each taker fill the adapter measures `(fill_price - rolling_mid) / rolling_mid * 10000` over the same `dt_seconds` the calibration uses, calls the model's `apply()` for the predicted bps, and exposes per-side / side-normalized-cost / predicted / predict-vs-realised-gap aggregates plus `n_samples` / `n_excluded` / `n_uncalibrated_predict` counters. `bts sweep` per-run line shows `slip <±cost_bps> n=<count>/excl=<count> gap <±gap_bps>bp/uncal=<count>`; the sweep ends with two summary lines (`sweep slippage  cost ...` and `sweep calibration  predict-vs-realised gap ...`) with WARN suffixes when coverage gap >10% or |gap| >1bp. `bts run --output-format json` emits the full set. Commits: 53ad757 (initial wiring), 6893b2d (audit fix 1: measure-only), d3c54bc (audit fix 2: sweep visibility, side-normalized cost), and the delta-closing follow-up (calibration fallback contract, sweep aggregate + coverage warning, calibration loop with predicted-vs-realised gap).
- [x] **Task 33.** Wire `MakerFillModel` into the launcher's LIMIT-OPEN handling. **Landed.** Final design (post-audit, 5 codex rounds): (a) calibration runs per-window in `cli/_run_window._calibrate_maker_fill` from the 30-min pre-window tape; raises `RuntimeError` on empty pre-window rather than silently falling back to the legacy first-crossing/full-fill path. (b) The venue's tape fetch is widened by `lookback_minutes` so each submit gets a fresh per-submit pre-window slice (codex round 1 P1 caught the prior single-window-start calibration). (c) `walk_trades` accepts `maker_model` and `trades_pre_submit`; `_walk_limit` ALWAYS routes through `MakerFillModel.evaluate` when the model is attached (codex round 1 P2 caught the prior first-tick taker reroute), aggregating multiple `ImmediateFill`s into ONE VWAP'd `FillResult` so `pair_trades` PnL is honest (codex round 2 P1). (d) Strategy template `long_on_signal` gains `maker_preference` baked-config; BUY entries become passive LIMITs while SELL exits stay MARKET (codex round 1 P1: asymmetric routing keeps `_long`/`_entry_qty` reconciliation honest). State now flows from `on_outcome` (not signal-emit) with `_pending_buy` / `_pending_sell` gates against duplicate emissions while async outcomes are in flight. (e) Action-side LIMIT touch refresh in `action_submitter._maybe_refresh_limit_to_touch` rewrites `execution_params['price']` to `touch ± tick` BEFORE validation (codex round 4 P2 caught the prior venue-side rewrite). (f) Telemetry: `n_limit_orders_submitted`, `n_limit_filled_full/partial/zero`, `n_limit_marketable_taker`, `n_passive_limits`, `maker_fill_efficiency_p50`, `maker_fill_efficiency_mean` on JSON; sweep summary `n=N full/part/zero=A/B/C  mkt_taker=D  passive=E  fill_eff_mean=X%` weighted by passive-LIMIT count (codex round 4 P2 fixed the mis-weighting). (g) `OutcomeLoop` wired into `BacktestLauncher` plus Praxis→Nexus `TradeOutcome` translator so `on_outcome` actually fires (codex round 2 P1: outcomes piled up in the queue without dispatch). (h) Launcher's adapter wrapper handles `OPEN` LIMIT zero-fill as EXPIRED for capital lifecycle. Commits: 2d3515f, 761af43, 9808fcf.

### Final phase (after all 28 tasks land)

- Codex full-PR review until codex approves the PR.
- Re-request `zero-bang`; address comments; re-request until zero-bang approves.
- Operator pinged once merge button is truly green.
